import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'builder.flipboard',
  appName: 'Departures',
  webDir: 'dist/builder-flipboard/browser'
};

export default config;
